---
description: Skill roots for Cursor, Claude Code, and other agents — AGENT_SKILLS_ROOT, flat skill layout.
alwaysApply: true
---

# Agent-agnostic skill paths

Skills live under a **configurable root**. Defaults:

| Environment | Default `AGENT_SKILLS_ROOT` |
|-------------|---------------------------|
| **ai-resources** (this repo) | `$AGENT_KIT/skills` (`AGENT_KIT` = repo root) |
| Claude Code | `~/.claude/skills` (typical) |
| Custom | `$AGENT_SKILLS_ROOT` if set |

## Layout

- **First-party skills:** `{root}/<category>/<skill>/SKILL.md` (e.g. `common/common-tdd/SKILL.md`).
- **Imported skills (flat):** `{root}/{skill_id}/SKILL.md` (e.g. `gpm-curated-typescript/SKILL.md`).
- **Shared references:** `{root}/_shared/*.md` (Engram / persistence excerpts).

## Install (any agent)

1. Update the kit (**`brew upgrade ai-resources`** or **`git pull`**), then run **`ai-resources generate`**.
2. **`ai-resources setup`** links each skill into the agent's native skills directory (`~/.claude/skills/<id>`, `~/.agents/skills/<id>`), writes the managed instruction block, and installs MCP and hooks.
3. Tools without native skill discovery read **`$AGENT_KIT/skills-index.json`** (paths relative to the kit root).

## ID → path (delegation)

- Flat id → `{root}/{id}/SKILL.md` (e.g. `gpm-curated-typescript`)

Workflow step resolution: skill **`kit-orchestration`**.
