---
# Machine-readable state. Keep these fields in sync with the notes below; a step reads them first.
workflow: ""            # workflow name, e.g. feature-implementation
step: ""                # step id that last wrote this file
status: pending         # pending | success | partial | blocked
blocked: false
return_to_step: ""      # step id to re-run when blocked
block_reason: ""
domain: ""              # dart-flutter | angular | symfony | api-platform | devops | security
requires_tests: unknown # true | false | unknown
security_critical: unknown
refs:                   # paths written by steps, not the content
  plan: ""
  architecture: ""
  test_report: ""
  review: ""
  security: ""
verdicts:               # pending until the owning step reports
  architect: pending    # yes | no | pending
  code_review: pending  # APPROVED | REQUIRES_CHANGES | pending
  security: pending     # PASS | CONDITIONAL | FAIL | N/A | pending
  verification: pending # PASS | GAPS | pending
next: ""                # step id to run next, or none
docs_updated: ""        # paths written by the document step, or none
git_integration: none   # merge_to_base | none | pending_human
---

<!-- The front matter above is authoritative: steps read it and the orchestrator joins parallel
     results into it. The notes below are commentary for humans — when they disagree, the front
     matter wins, and whoever wrote the note should fix it. -->


# Handoff

**Status:** (success | partial | blocked)

**Executive summary:** (1–3 sentences; ATL-compatible)

**Goal reached:** (1–2 sentences)

**Changes made:** (list file paths only, no code)
- 

**Commands run:** (one line each, or "none")
- 

**Unresolved / risks:** (open items or blockages)
- 

**Next recommended:** (next workflow step / SDD phase, or "none")

**Next assigned role:** (planner | implementer | tester | verifier | software-architect | code-reviewer | security-auditor | explore | doc-writer | generalPurpose)

**Domain:** (dart-flutter | angular | symfony | api-platform | devops | security)

**Blocked:** (true | false)
**Return_to_step:** (step id to re-invoke if Blocked; e.g. plan, architect, implement)
**Block_reason:** (brief reason when Blocked=true)

---

## Implementation Workflow Fields

**Plan_ref:** (path to YYMMDD-NNN.md in .agent-output/planner/)
**Architect_approval:** (yes | no | pending)
**Test_report:** (path to YYMMDD-NNN.md in .agent-output/tester/ if applicable)
**Code_review_verdict:** (APPROVED | REQUIRES_CHANGES | pending)
**Security_audit:** (PASS | CONDITIONAL | FAIL | N/A)
**Spec_refs:** (list of spec paths read or modified in this step)
**Requirements_preserved:** (key constraints from specs that downstream steps must respect)
**Engram_refs:** (Engram topic keys for detailed context, if available)
**Requires_tests:** (yes | no)
**Security_critical_feature:** (yes | no)

**Git_integration:** `merge_to_base` (worktree workflows) | `none` | `pending_human` — With worktree: on completion, finishing-a-development-branch merges to base branch by default. Set `pending_human` when the user wants to review before merge. Report branch + worktree path.

---

*Copy this template to `.agent-output/handoff-<branch>.md`. Update the front matter and the notes after each step; the next agent continues from the handoff + referenced files only. Workflows in `$AGENT_KIT/workflows/`; working knowledge artifacts in `<repo>/specs/knowledge/` (research, decisions, searchable).*

**When task DONE:** DELETE the handoff file. No pending handoffs, zombies, or orphans.
