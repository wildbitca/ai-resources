---
description: Project bootstrap — how agents resolve config, find workflows, personas, and project rules.
alwaysApply: true
---
# Project Bootstrap

Defines how agents discover and use shared (global kit) and project-specific resources.

**`$AGENT_KIT`** is the root of the **ai-resources** repository (rules, workflows, skills). Set via `export AGENT_KIT=...` or by running `bin/agent-kit` (auto-detects). All paths below under **`cursor/`** are **`$AGENT_KIT/cursor/...`**.

## Configuration resolution

| Scope | Path | Contents |
|-------|------|----------|
| **Global (shared)** | `$AGENT_KIT/cursor/` | Rules, roles, personas, skills, workflows, domain commands, templates, AGENTS.md, handoff template. Git-tracked in **ai-resources**. |
| **Project** | `<repo>/.cursor/` | Only `mcp.json` (required by Cursor IDE). |
| **Project root** | `<repo>/` | `specs/PROJECT.md` (intent, stack, index — **primary** business context), optional `.cursorrules`, `specs/` (feature specs + ADRs; working artifacts in `specs/knowledge/`). |

**Resolution order:** Project `specs/PROJECT.md` and optional `.cursorrules` > Global rules from **`$AGENT_KIT/cursor/rules/`**. Everything else lives in the kit repo.

## Path conventions

| Resource | Path |
|----------|------|
| Rules | `$AGENT_KIT/cursor/rules/*.mdc` |
| Roles | `$AGENT_KIT/cursor/agents/roles/{role}.md` |
| Personas | `$AGENT_KIT/cursor/agents/personas/{role}-{domain}.md` |
| Skills | `$AGENT_KIT/cursor/skills/**/SKILL.md` (includes `vendor/gentleman-programming/`) |
| Skill index | `$AGENT_KIT/cursor/AGENTS.md` |
| Workflows | `$AGENT_KIT/cursor/workflows/*.workflow.yaml` |
| Domain commands | `$AGENT_KIT/cursor/workflows/_domain-commands.yaml` |
| Handoff template | `$AGENT_KIT/cursor/handoff.md.template` |
| Spec/ADR templates | `$AGENT_KIT/cursor/templates/` |
| MCP config | `<repo>/.cursor/mcp.json` (project-specific) |
| Business config | `<repo>/specs/PROJECT.md` (primary); optional `<repo>/.cursorrules` |
| Env files | Classify by **content**, not filename — see `env-files-context.mdc` |
| Specs | `<repo>/specs/` (project-specific) |
| Knowledge (working) | `<repo>/specs/knowledge/` — research, decisions, audits (not canonical product spec; prune after promotion) |

## Agent persona loading

When a workflow step specifies a subagent_type (e.g. implementer):
1. Look for `$AGENT_KIT/cursor/agents/personas/{role}-{domain}.md` (domain-specific)
2. Fall back to `$AGENT_KIT/cursor/agents/roles/{role}.md` (universal role definition)

## Skill discovery

Skills index at **`$AGENT_KIT/cursor/AGENTS.md`**. Skills at **`$AGENT_KIT/cursor/skills/`**. Rule `agent-skill-standard-rule.mdc` enforces discovery.

## Knowledge directory (under specs)

Project-specific, at `<repo>/specs/knowledge/` (centralized with documentation; **not** the product source of truth — that is `specs/features/`, `specs/cross-cutting/`, `specs/architecture/`):
- `research/` — codebase exploration, bug analysis
- `decisions/` — plans, architecture decisions
- `searchable/` — validation reports

After content is promoted into specs, obsolete files here must be removed in the **same workflow run** (SDD closure). Command `/knowledge-audit` or skill `knowledge-audit` — see **`$AGENT_KIT/cursor/skills/knowledge-audit/SKILL.md`** (SDD closure table + `references/audit-detail.md`). Workflows that write `specs/knowledge/` attach this skill to the final verify/plan/report step.

## Handoff protocol

See `050-subagent-delegation.mdc`. Handoff files: `<repo>/.agent-output/handoff-{scope}.md`. Template: **`$AGENT_KIT/cursor/handoff.md.template`**.

## Commands

| Command | Rule | What it does |
|---------|------|-------------|
| `/self-update` | `self-update.mdc` | `agent-kit self-update` — git pull **ai-resources** + vendor skills + `skills-registry` / `skills-index.json` |
| `/setup-project` | `setup-project.mdc` | Scaffold a new project with `specs/PROJECT.md`, specs/, specs/knowledge/; optional `.cursorrules` |
| `/knowledge-audit` | `knowledge-audit.mdc` | Audit/prune `specs/knowledge/` after promotion to specs (skill: `knowledge-audit`) |
| `/delegate` | `delegate.mdc` | Strict orchestrator-only: workflows + `Task`, minimal main-thread output |

## No-go zones

Check `specs/PROJECT.md` and linked feature/cross-cutting specs for project-specific prohibitions.
